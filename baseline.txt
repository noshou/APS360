Zero-order (no molecule information)
1. MeanBaseline — training set mean I(q) for every molecule
2. PerSizeMeanBaseline — bucket by atom count M, predict mean I(q) for that bucket

Chemistry only (no geometry)
3. CompositionBaseline — atom type counts → I(q), no coordinates
4. FormFactorBaseline — mean |f(q)| per molecule weighted by element, no geometry

Geometry, global (single length scale)
5. RgBaseline — Guinier fit, valid low q only
6. PowerLawBaseline — fit A·q^(-α) per molecule class, valid high q only

Geometry, local (multiple length scales)
7. PairPeakBaseline — peak of P(r) histogram → oscillation frequency, mid q
8. NearestNeighbourBaseline — mean nearest-neighbour distance, high q

ScatterNet ablations (not baselines, but go in same eval loop)
9. FixedSigmaAblation — ScatterNet with sigma=1 everywhere
10. NoFormFactorPenaltyAblation — ScatterNet trained without λ₆ penalty

What beating each one proves:

┌──────────┬──────────────────────────────────┐
│ Baseline │              Proves              │
├──────────┼──────────────────────────────────┤
│ 1        │ model is molecule-sensitive      │
├──────────┼──────────────────────────────────┤
│ 2        │ model uses more than size        │
├──────────┼──────────────────────────────────┤
│ 3/4      │ model uses 3D geometry           │
├──────────┼──────────────────────────────────┤
│ 5        │ model captures more than Rg      │
├──────────┼──────────────────────────────────┤
│ 6/7/8    │ model works across q range       │
├──────────┼──────────────────────────────────┤
│ 9        │ sigma is doing real work         │
├──────────┼──────────────────────────────────┤
│ 10       │ form factor regularization helps │
└──────────┴──────────────────────────────────┘