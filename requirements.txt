# APS360 — Python dependencies
# Install into venv: pip install -r requirements.txt

# Core numerics
numpy>=2.0
scipy>=1.17

# Runtime type checking
beartype>=0.22

# X-ray form factors (f0 Cromer-Mann/Waasmaier, f1/f2 Chantler)
xraydb>=4.5

# Crystallography file I/O (mmCIF → XYZ, used in RCSB/viro3D extraction)
gemmi>=0.7

# Demo visualisation
plotly>=6.0

# QM9 / tmQM dataset loader (extract_to_xyz.py)
atomic-datasets>=0.1

# Tabular data / Parquet (sources.parquet, groups.parquet pipeline)
pandas>=2.0

# HDF5 I/O (dataset.h5 feature store)
h5py>=3.0
